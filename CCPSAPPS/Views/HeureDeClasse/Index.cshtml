@*@model IEnumerable<CCPSAPPS.Models.HeuresDeClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.HeureDescription)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categorie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.HeureDescription)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categorie)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HeureID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HeureID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HeureID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
*@